apiVersion: v1
kind: Service
metadata:
  name: notificationsservice
spec:
  selector:
    app: notificationsservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
